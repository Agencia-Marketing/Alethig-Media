dist/
.astro/
node_modules/
npm-debug.log*
.env
.env.production
.dev.vars
.DS_Store
.idea/
.wrangler/

# Materiales fuente de marca (no van al repo del sitio)
Alethig Media-Catálogo.pdf
/*.png
SVG/
TIPOGRAFIAS/

# Variantes responsive generadas en build-time por
# scripts/generate-responsive-images.mjs (ver npm run prebuild) —
# se regeneran en cada build, no se versionan.
public/uploads/*-[0-9]*w.webp
public/uploads/_responsive-manifest.json
